{
  "name": "@balance/pluginctl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@balance/plugin-codegen": "workspace:*",
    "pacote": "^21.0.3",
    "tar": "^7.5.1",
    "zod": "^4.1.12"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
